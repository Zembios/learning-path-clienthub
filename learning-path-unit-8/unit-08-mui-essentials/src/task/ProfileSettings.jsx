import React, { useState } from "react";
import { Box, Typography, TextField, Select, MenuItem, Button, Autocomplete } from "@mui/material";

/**
 * ProfileSettings (Starter)
 * Requirements in theory+task.md
 */
export function ProfileSettings(/* { roles, cities, defaultCity, highlight, onSave } */) {
  // TODO: implement controlled Name (TextField), Role (Select with native or MenuItem), City (Autocomplete), and Save button
  // Use Box with responsive & conditional sx.
  throw new Error("ProfileSettings not implemented");
}
