import React, { useEffect, useMemo, useCallback, useRef, useState } from "react";

/**
 * Dashboard (Starter)
 * Requirements in theory+task.md
 */
export function Dashboard(/* { fetcher, intervalMs = 5000 } */) {
  // TODO: implement polling, memoized total, stable callback, and a ref use
  throw new Error("Dashboard not implemented");
}
